CREATE DATABASE pixel_game;
CREATE TABLE image_game_link (image_path varchar(255), game_name varchar(510));
CREATE USER pixel WITH ENCRYPTED PASSWORD 'PASSWORD';
GRANT ALL PRIVILEGES ON DATABASE pixel_game TO pixel; # TODO create 2 users, one for import and other for the game wich should be read only
